{
  "schema_version": 2,
  "defaults": {
    "schema_version": 2,
    "app": {
      "env_prefix": null,
      "config": false,
      "config_format": "json",
      "config_path": null,
      "config_conflict_mode": "cli-wins",
      "proc_observe_allowlist": [],
      "global_flags": [],
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tag_contracts": {},
      "checks": {},
      "config_fields": {},
      "infra": {}
    },
    "flag": {
      "short": null,
      "env": null,
      "env_separator": null,
      "prefixed": true,
      "choices": null,
      "elect_by": null,
      "unique": false,
      "conflict_mode": null,
      "negatable": null,
      "nullable": false
    },
    "arg": {
      "variadic": false,
      "choices": null
    },
    "choice": {
      "flags": []
    },
    "choice_record": {
      "help": null
    },
    "command": {
      "consequential": false,
      "dry_run_supported": true,
      "dry_run_unsupported_reason": null,
      "update_of": null,
      "write_mode": null,
      "payload_schema": null,
      "owns_stdout": false,
      "passthrough": false,
      "flags": [],
      "flag_sets": [],
      "args": [],
      "tags": [],
      "constraints": [],
      "hidden": false,
      "interactive": false,
      "config_fields": [],
      "grants": [],
      "forwarding": null
    },
    "group": {
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tags": [],
      "hidden": false
    },
    "config_field": {
      "default": null,
      "bound_commands": []
    },
    "check": {
      "scope": null
    },
    "infra": {
      "roots": [],
      "handshakes": [],
      "connections": []
    }
  },
  "project_id": "claudestream",
  "name": "claudestream",
  "version": "0.15.1",
  "help": "Stream Claude Code's JSON protocol",
  "commands": {
    "send": {
      "name": "send",
      "help": "Send a prompt to Claude and display the complete response with events",
      "effect": "mutating",
      "flags": [
        {
          "name": "model",
          "help": "Claude model identifier for this session (e.g. sonnet, opus)",
          "value_schema": {
            "type": "string"
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "cwd",
          "help": "Working directory for the Claude Code subprocess to operate in; when omitted the subprocess inherits this process's directory",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "raw",
          "help": "Show raw protocol events from the subprocess instead of flattened output (flattened when neither --raw nor --no-raw is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "json-output",
          "help": "Serialize each protocol event as a JSON line on stdout (human rendering when neither --json-output nor --no-json-output is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "skip-permissions",
          "help": "Bypass all tool permission prompts via --dangerously-skip-permissions (permissions enforced when neither --skip-permissions nor --no-skip-permissions is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to use for authentication",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "footer",
          "help": "Display cost and timing summary on stderr after completion (displayed when neither --footer nor --no-footer is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "system-prompt",
          "help": "Custom system prompt text to prepend to the Claude session; when omitted the session carries no custom system prompt",
          "value_schema": {
            "type": "string"
          },
          "short": "s",
          "presence": "optional"
        },
        {
          "name": "stdin",
          "help": "Read the prompt text from standard input instead of an argument (the prompt argument is used when neither --stdin nor --no-stdin is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "color",
          "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "resume",
          "help": "Resume a previously started Claude session by its unique session ID; when omitted a new session is started",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "from-pr",
          "help": "Load context from a GitHub pull request identifier to resume; when omitted no pull request context is loaded",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "args": [
        {
          "name": "prompt",
          "help": "The text prompt to send to the Claude Code session; omit it and pass --stdin to read the prompt from standard input",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "stream": {
      "name": "stream",
      "help": "Stream a prompt with real-time incremental token-by-token output to stdout",
      "effect": "mutating",
      "flags": [
        {
          "name": "model",
          "help": "Claude model identifier to use for this session (e.g. sonnet)",
          "value_schema": {
            "type": "string"
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "cwd",
          "help": "Working directory for the Claude Code subprocess to operate in; when omitted the subprocess inherits this process's directory",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "skip-permissions",
          "help": "Bypass all tool permission prompts via --dangerously-skip-permissions (permissions enforced when neither --skip-permissions nor --no-skip-permissions is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to use for authentication",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "footer",
          "help": "Display cost and timing summary on stderr after completion (displayed when neither --footer nor --no-footer is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "system-prompt",
          "help": "Custom system prompt text to prepend to the Claude session; when omitted the session carries no custom system prompt",
          "value_schema": {
            "type": "string"
          },
          "short": "s",
          "presence": "optional"
        },
        {
          "name": "stdin",
          "help": "Read the prompt text from standard input instead of an argument (the prompt argument is used when neither --stdin nor --no-stdin is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "color",
          "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "resume",
          "help": "Resume a previously started Claude session by its unique session ID; when omitted a new session is started",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "from-pr",
          "help": "Load context from a GitHub pull request identifier to resume; when omitted no pull request context is loaded",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "args": [
        {
          "name": "prompt",
          "help": "The text prompt to send to the Claude Code session; omit it and pass --stdin to read the prompt from standard input",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "events": {
      "name": "events",
      "help": "Debug mode: display all raw JSON protocol events from the subprocess",
      "effect": "mutating",
      "flags": [
        {
          "name": "model",
          "help": "Claude model identifier to use for this session (e.g. sonnet)",
          "value_schema": {
            "type": "string"
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "cwd",
          "help": "Working directory for the Claude Code subprocess to operate in; when omitted the subprocess inherits this process's directory",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "skip-permissions",
          "help": "Bypass all tool permission prompts via --dangerously-skip-permissions (permissions enforced when neither --skip-permissions nor --no-skip-permissions is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to use for authentication",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "footer",
          "help": "Display cost and timing summary on stderr after completion (displayed when neither --footer nor --no-footer is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "system-prompt",
          "help": "Custom system prompt text to prepend to the Claude session; when omitted the session carries no custom system prompt",
          "value_schema": {
            "type": "string"
          },
          "short": "s",
          "presence": "optional"
        },
        {
          "name": "stdin",
          "help": "Read the prompt text from standard input instead of an argument (the prompt argument is used when neither --stdin nor --no-stdin is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "color",
          "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "resume",
          "help": "Resume a previously started Claude session by its unique session ID; when omitted a new session is started",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "from-pr",
          "help": "Load context from a GitHub pull request identifier to resume; when omitted no pull request context is loaded",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "args": [
        {
          "name": "prompt",
          "help": "The text prompt to send to the Claude Code session; omit it and pass --stdin to read the prompt from standard input",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "repl": {
      "name": "repl",
      "help": "Start an interactive multi-turn read-eval-print loop session with Claude",
      "effect": "mutating",
      "flags": [
        {
          "name": "model",
          "help": "Claude model identifier to use for this session (e.g. sonnet)",
          "value_schema": {
            "type": "string"
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "cwd",
          "help": "Working directory for the Claude Code subprocess to operate in; when omitted the subprocess inherits this process's directory",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "skip-permissions",
          "help": "Bypass all tool permission prompts via --dangerously-skip-permissions (permissions enforced when neither --skip-permissions nor --no-skip-permissions is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to use for authentication",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "footer",
          "help": "Display cost and timing summary on stderr after completion (displayed when neither --footer nor --no-footer is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "system-prompt",
          "help": "Custom system prompt text to prepend to the Claude session; when omitted the session carries no custom system prompt",
          "value_schema": {
            "type": "string"
          },
          "short": "s",
          "presence": "optional"
        },
        {
          "name": "color",
          "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "resume",
          "help": "Resume a previously started Claude session by its unique session ID; when omitted a new session is started",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "from-pr",
          "help": "Load context from a GitHub pull request identifier to resume; when omitted no pull request context is loaded",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "ask": {
      "name": "ask",
      "help": "Send a prompt to Claude and print only the final response text",
      "effect": "mutating",
      "flags": [
        {
          "name": "model",
          "help": "Claude model identifier to use for this session (e.g. sonnet)",
          "value_schema": {
            "type": "string"
          },
          "short": "m",
          "presence": "required"
        },
        {
          "name": "profile",
          "help": "Name of the claudewheel profile for authentication",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "cwd",
          "help": "Working directory path for the Claude Code process to operate in; when omitted the subprocess inherits this process's directory",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "skip-permissions",
          "help": "Bypass all tool permission prompts via --dangerously-skip-permissions (permissions enforced when neither --skip-permissions nor --no-skip-permissions is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "system-prompt",
          "help": "Custom system prompt text to prepend to the session context; when omitted the session carries no custom system prompt",
          "value_schema": {
            "type": "string"
          },
          "short": "s",
          "presence": "optional"
        },
        {
          "name": "stdin",
          "help": "Read the prompt text from standard input instead of an argument (the prompt argument is used when neither --stdin nor --no-stdin is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "json-output",
          "help": "Serialize the AskResult response object as a JSON line on stdout (the response text alone when neither --json-output nor --no-json-output is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "color",
          "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "from-pr",
          "help": "Load context from a GitHub pull request identifier to resume; when omitted no pull request context is loaded",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ],
      "args": [
        {
          "name": "prompt",
          "help": "The text prompt to send to the Claude Code session; omit it and pass --stdin to read the prompt from standard input",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "doctor": {
      "name": "doctor",
      "help": "Check claudestream environment health: binary, version, and profile",
      "effect": "read_only",
      "flags": [
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to validate and check; when omitted no profile is resolved",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "config": {
      "name": "config",
      "help": "Show resolved configuration including binary path and version",
      "effect": "read_only",
      "flags": [
        {
          "name": "profile",
          "help": "Name of the claudewheel profile to display settings for; when omitted no profile is resolved",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    }
  },
  "groups": {
    "agent": {
      "name": "agent",
      "help": "Manage and run agents defined in .agent.json files. Agent definitions declare a model, prompt template, allowed tools with input schemas, sandbox permissions, and budget limits (cost, turns, tokens). Use subcommands to validate configurations, run agents against prompts, and inspect metadata.",
      "commands": {
        "run": {
          "name": "run",
          "help": "Load an agent definition and run it with the given prompt. Accepts a path to a .agent.json file or a bare agent name (resolved from .claudestream/agents/). The definition specifies the model, a prompt template with {variable} placeholders, tool schemas, sandbox policy, and budget constraints. Use --var key=value to substitute template variables. Use --model to override the model declared in the definition.",
          "effect": "mutating",
          "flags": [
            {
              "name": "var",
              "help": "Template variable in key=value format, repeatable for multiple variables",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "default",
              "default": []
            },
            {
              "name": "model",
              "help": "Override the model declared in the agent definition file; when omitted the definition's own model is used",
              "value_schema": {
                "type": "string"
              },
              "short": "m",
              "presence": "optional"
            },
            {
              "name": "profile",
              "help": "Name of the claudewheel profile to use for authentication",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "cwd",
              "help": "Working directory path for the Claude Code process to operate in; when omitted the subprocess inherits this process's directory",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "footer",
              "help": "Display cost and timing summary on stderr after completion (displayed when neither --footer nor --no-footer is passed)",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            },
            {
              "name": "color",
              "help": "Enable ANSI colored output for terminal display and formatting (enabled, subject to TTY and NO_COLOR detection, when neither --color nor --no-color is passed)",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            }
          ],
          "args": [
            {
              "name": "definition",
              "help": "Agent name or filesystem path to a .agent.json definition file",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "prompt",
              "help": "User message prompt to send to the agent for processing",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "list": {
          "name": "list",
          "help": "List available agents from .claudestream/agents/. Scans the agents directory in the working directory (or the directory specified by --cwd) and prints a table with each agent's name, schema version, and description. Use this to discover which agents are configured before running one with 'agent run'.",
          "effect": "read_only",
          "flags": [
            {
              "name": "cwd",
              "help": "Working directory path to scan for agent definitions; when omitted the current directory is scanned",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        },
        "info": {
          "name": "info",
          "help": "Display agent definition details for a given agent name or path. Loads the .agent.json file, parses it, and prints every configured field: name, version, description, model, budget limits, sandbox policy, tool schemas, MCP server config, and stream options. Use this to inspect an agent's full configuration before invoking it.",
          "effect": "read_only",
          "args": [
            {
              "name": "name",
              "help": "Agent name or filesystem path to the .agent.json definition",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "validate": {
          "name": "validate",
          "help": "Validate an agent definition by loading and checking its .agent.json file for structural and semantic correctness. Verifies that budget values are non-negative, the prompt template is non-empty, tool schemas are well-formed, and required fields are present. Reports specific errors on failure or prints a success confirmation.",
          "effect": "read_only",
          "args": [
            {
              "name": "name",
              "help": "Agent name or filesystem path to the .agent.json definition",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        }
      }
    }
  }
}
