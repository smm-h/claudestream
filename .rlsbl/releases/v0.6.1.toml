# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.6.1
description = "**Session resumption.** New `resume_session_id` parameter and `--resume` CLI flag to continue a previous Claude Code session."
candidate_sha = "61a61d7fa0594c74cb105fef09b2e7f04ce8a7f3"

[tree_hashes]
"." = "8e647f86de6f1698467c575246be27b9b924de33"
