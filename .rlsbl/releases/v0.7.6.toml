# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi", "npm"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.7.6
description = "**Fixed MCP tool visibility.** Tools registered via `@tool` are now visible to the Claude Code model. The `--permission-prompt-tool stdio` flag was missing when MCP tools were registered without sandbox restrictions."
candidate_sha = "3e104cfa42e72ed48843f675fcdd34fccbdec910"

[tree_hashes]
"." = "44dd591df64bcfe236759cb051944e04f041efad"
