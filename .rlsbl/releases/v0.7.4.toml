# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.7.4
description = "**Fixed prompt template false positives.** `resolve_prompt()` no longer flags `{word}` patterns that were introduced by variable substitution itself."
candidate_sha = "4da44a173953d02a9aeff803f9e086b0e14d3323"

[tree_hashes]
"." = "cfee2cbc736fd5659de6114177d4374c7be92054"
