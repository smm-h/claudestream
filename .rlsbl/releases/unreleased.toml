# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Transparent subprocess retry and structured logging"
# Optional context explaining why these changes were made
context = "Adds automatic subprocess retry on stuck detection and structured logging for liveness probe events."
include = ["pypi", "npm"]
exclude = []
