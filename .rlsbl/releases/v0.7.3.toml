# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.7.3
description = "**Agent discovery from custom paths and packages.** `discover_agents()` now accepts `paths=` for custom directories and `packages=` for Python package resources via `importlib.resources`. Deduplicates by name with warnings on conflicts. `load_agent()` gains a `cwd` parameter for consistent bare name resolution."
candidate_sha = "62323ccd33e7609be27fc68f225c6750874ec453"

[tree_hashes]
"." = "d2f257260f3faf11fcce4fd0cbd0e29f72f61de7"
