# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi", "npm"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.7.7
description = "**`--from-pr` CLI flag.** All commands (send, stream, events, repl, ask) now accept `--from-pr` to resume from a PR-linked session."
candidate_sha = "00754c08252e3e1f16d5a73aaaa2a3d7592af858"

[tree_hashes]
"." = "f5004e420df15da85026ec785550f5dfb0caa296"
