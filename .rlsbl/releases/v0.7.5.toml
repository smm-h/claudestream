# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi", "npm"]
exclude = []
# description reconstructed by `rlsbl release backfill` from github-release:v0.7.5
description = "**npm package.** `npx claudestream` now works. The npm package is a thin CLI wrapper that shells out to the Python `claudestream` command."
candidate_sha = "a539dfc69c37dcb9d556772eabe6fabde9202939"

[tree_hashes]
"." = "df91b27dbc44b99ea0115fe5206312f5cec7e1d0"
